# App settings
APP_NAME=ShopFlow
SECRET_KEY=fwf23232&%^&%^ggcfCF^R^%^ffTYC   
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60          

# Database
# Leave MONGODB_URL blank = uses in-memory store (data resets on restart)
# Fill it in = data persists to MongoDB Atlas
MONGODB_URL=
DB_NAME=shopflow
USE_FIRESTORE=false                    

# Email — leave blank for now, emails will print to terminal instead
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@shopflow.com
FROM_NAME=ShopFlow

# CORS — who can call the API
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000

# Rate limiting
RATE_LIMIT_PER_MINUTE=60